import { inspect, performanceTest } from './lib/util';
import { genArray } from './lib/data_generators';

const name: string = 'Fanny';

if (name === 'Lily') {
  console.log(`Hello ${name}`);
} else {
  console.log('I dont know you, can you go away please!');
}
